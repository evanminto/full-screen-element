{
  "name": "@evanminto/full-screen-element",
  "version": "0.1.0",
  "description": "",
  "source": "src/index.js",
  "browser": "dist/common.js",
  "module": "dist/module.js",
  "targets": {
    "default": {
      "context": "browser",
      "outputFormat": "global"
    }
  },
  "scripts": {
    "watch": "parcel watch",
    "build": "parcel build",
    "demo": "yarn run demo:default",
    "demo:default": "yarn parcel demo/default.html --dist-dir demo/dist/",
    "demo:with-target": "yarn parcel demo/with-target.html --dist-dir demo/dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Evan Minto (vamptvo@gmail.com)",
  "license": "MIT",
  "dependencies": {
    "lit": "^2.2.1"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "parcel": "^2.4.0",
    "prettier": "^2.6.0"
  }
}
